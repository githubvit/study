
服务器 
socket工具
model工具 数据库连接 增删改查 
用户工具 jwt验证 业务权限

一 项目的模块化与解耦
    总线模块（装配模块）
        通用模块
        用来耦合各个模块

    收支模块
        通用模块
        接入第三方的收支业务api（微信或支付宝等）。

        响应：
            收到：
                某第三方提供的某用户支付成功或失败的信息。
                为 何种业务 的支付。
                开通 还是 续费。

            输出：
                某第三方提供的某用户支付成功或失败的信息。
                为 何种业务 的支付。
                开通 还是 续费。
        透传。

        测试收支模块的代码，包含各种用例。
        
    用户模块
        通用模块
        为用户登录和注册的模块。
        获取用户的相关信息。用户的地理信息、操作系统信息、使用的软件版本信息、用户的登录时间等。
        用户的支付信息。
        用户的业务信息。

        输入：
            用户名 密码
        
        输出：
            用户登录成功或失败的信息。
            该用户的业务权限。

        测试用户模块的代码，包含各种用例。

    业务模块
        非通用模块。
        实现某具体的业务逻辑。

        测试某具体业务逻辑的代码，包含各种用例。

    

二 技术基础
    数据库
    socket
    协程
    爬虫
    pyside2